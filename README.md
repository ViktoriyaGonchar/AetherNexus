# AetherNexus — Интеллектуальная Ткань Знаний

**PyCharm Plugin Edition**

[![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)](https://github.com/aethernexus/aethernexus)
[![Python](https://img.shields.io/badge/python-3.10+-green.svg)](https://www.python.org/)
[![Kotlin](https://img.shields.io/badge/kotlin-1.9+-orange.svg)](https://kotlinlang.org/)
[![License](https://img.shields.io/badge/license-MIT-lightgrey.svg)](LICENSE)

## 📋 Описание

**AetherNexus** — это интеллектуальная система знаний, встроенная напрямую в PyCharm IDE. Плагин преобразует код, комментарии и документацию в контекстуализированную, запрашиваемую базу знаний, доступную без переключения контекста.

> 🎬 **[Смотреть интерактивную демонстрацию](demo/index.html)** | **[Демо документация](Демо.md)**

### 🎯 Проблема

Разработчики теряют до **40% времени** на поиск информации в проекте: документации, конфигах, legacy-коде и смежных задачах. Знания фрагментированы между файлами, PR, тикетами и командными чатами.

### ✨ Решение

Встроить интеллектуальный слой знаний напрямую в IDE, превратив её в центральный узел для доступа ко всей информации проекта.

### 📊 Измеримая Ценность

- ⚡ **Сокращение времени поиска информации** на **70%**
- 🔄 **Снижение контекстных переключений** на **60%**
- 🚀 **Ускорение онбординга новых разработчиков** на **50%**
- 🔗 **Автоматическое обнаружение связей** между кодом, ошибками и документацией

---

## 🏗️ Архитектура

### Компоненты

1. **Backend (FastAPI)** — микросервисный бэкенд с событийной архитектурой
2. **PyCharm Plugin (Kotlin)** — интегрированный плагин для IDE
3. **Vector Database (Qdrant)** — хранение эмбеддингов для семантического поиска
4. **Graph Database (Neo4j)** — граф связей между сущностями
5. **Cache (Redis)** — кеширование запросов

### Ключевые Принципы

- 🏠 **Локально-первый подход**: Возможность работы с локальной копией бэкенда
- 📊 **Графо-ориентированное представление**: Связывание сущностей (классы, функции, тикеты, PR)
- 🔌 **API-first дизайн**: Легкий SDK для интеграции
- 🎨 **Минималистичный UI**: Ненавязчивый интерфейс внутри PyCharm
- 📴 **Оффлайн-работа**: Полная работа без интернета с синхронизацией по требованию

---

## 🚀 Быстрый Старт

### Требования

- Python 3.10+
- JDK 17+
- Gradle 8.0+
- PyCharm Community 2023.1+
- Qdrant (для векторного поиска)
- Neo4j (для графа связей)
- Redis (для кеширования)

### Установка Backend

```bash
# 1. Клонировать репозиторий
git clone https://github.com/aethernexus/aethernexus.git
cd aethernexus

# 2. Создать виртуальное окружение
python -m venv .venv

# 3. Активировать виртуальное окружение
# Windows:
.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Создать файл .env (скопировать из .env.example)
cp backend/.env.example backend/.env
# Отредактировать backend/.env с вашими настройками

# 6. Запустить бэкенд
cd backend
python main.py
```

Backend будет доступен по адресу: `http://localhost:8000`

API документация: `http://localhost:8000/api/docs`

### Установка PyCharm Plugin

#### Способ 1: Из исходников (для разработки)

```bash
cd pycharm-plugin

# Собрать плагин
./gradlew buildPlugin

# Запустить PyCharm с плагином
./gradlew runIde
```

#### Способ 2: Установка в существующий PyCharm

```bash
cd pycharm-plugin
./gradlew buildPlugin
```

Затем в PyCharm:
1. `File` → `Settings` → `Plugins`
2. Нажмите ⚙️ → `Install Plugin from Disk`
3. Выберите `pycharm-plugin/build/distributions/AetherNexus-*.zip`

---

## 📖 Использование

### Настройка

1. Откройте `File` → `Settings` → `Tools` → `AetherNexus`
2. Укажите URL бэкенда (по умолчанию: `http://localhost:8000/api/v1`)
3. При необходимости укажите API ключ
4. Включите/выключите автоматическую индексацию

### Основные Функции

#### 🔍 Поиск

- Откройте панель AetherNexus: `Tools` → `Show AetherNexus` (или `Ctrl+Alt+A`)
- Введите запрос в поисковую строку
- Выберите тип поиска: текстовый, семантический или по графу

#### 💡 Объяснение Кода

1. Выделите фрагмент кода
2. Правый клик → `Explain this code` (или `Ctrl+Alt+E`)
3. Получите объяснение на основе контекста проекта

#### 🔗 Поиск Связанных Сущностей

1. Правый клик на файле/методе → `Find related tickets`
2. Просмотрите связанные тикеты, PR и документацию

#### 📝 Генерация Документации

1. Выделите функцию или класс
2. Правый клик → `Generate documentation`
3. Вставьте сгенерированный docstring в код

#### 📊 Визуализация Графа

- Откройте граф связей для выбранной сущности
- Интерактивная навигация по связям
- Фильтрация и поиск в графе

---

## 🛠️ Разработка

### Структура Проекта

```
AetherNexus/
├── backend/                    # Backend сервисы
│   ├── app/
│   │   ├── api/               # API endpoints
│   │   │   └── v1/
│   │   │       └── endpoints/
│   │   ├── core/              # Конфигурация, логирование
│   │   ├── services/          # Бизнес-логика
│   │   └── models/            # Модели данных
│   ├── main.py                # Точка входа
│   └── requirements.txt        # Python зависимости
│
├── pycharm-plugin/            # PyCharm Plugin
│   ├── src/
│   │   └── main/
│   │       ├── kotlin/
│   │       │   └── com/aethernexus/
│   │       │       ├── ui/           # UI компоненты
│   │       │       ├── api/          # API клиент
│   │       │       ├── actions/      # Действия плагина
│   │       │       ├── settings/     # Настройки
│   │       │       └── listeners/    # Слушатели событий
│   │       └── resources/
│   │           └── META-INF/
│   │               └── plugin.xml    # Конфигурация плагина
│   └── build.gradle.kts       # Gradle конфигурация
│
├── docs/                      # Документация
├── REQUIREMENTS.md            # Детальные требования
├── INSTALL.md                 # Инструкции по установке
└── README.md                  # Этот файл
```

### Запуск Backend для разработки

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Сборка Plugin для разработки

```bash
cd pycharm-plugin
./gradlew runIde
```

### Тестирование

```bash
# Backend тесты
cd backend
pytest

# Plugin тесты
cd pycharm-plugin
./gradlew test
```

---

## 📡 API Endpoints

### Аутентификация

- `POST /api/v1/auth/login` — Авторизация
- `GET /api/v1/auth/validate` — Проверка токена
- `POST /api/v1/auth/refresh` — Обновление токена

### Поиск

- `POST /api/v1/search/text` — Текстовый поиск
- `POST /api/v1/search/semantic` — Семантический поиск
- `POST /api/v1/search/graph` — Поиск по графу
- `GET /api/v1/search/history` — История запросов

### Индексация

- `POST /api/v1/index/project` — Запуск индексации
- `GET /api/v1/index/status` — Статус индексации
- `DELETE /api/v1/index/project/{project_id}` — Удаление индекса

### Контекст

- `POST /api/v1/context/explain` — Объяснение кода
- `GET /api/v1/context/related/{entity_id}` — Связанные сущности
- `POST /api/v1/context/generate-docs` — Генерация документации

### Граф

- `GET /api/v1/graph/entity/{entity_id}` — Граф для сущности
- `GET /api/v1/graph/connections/{entity_id}` — Связи сущности

Полная документация API доступна по адресу: `http://localhost:8000/api/docs`

---

## 🔧 Конфигурация

### Backend (.env)

```env
# Основные настройки
DEBUG=True
HOST=0.0.0.0
PORT=8000

# Безопасность
SECRET_KEY=your-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Базы данных
QDRANT_HOST=localhost
QDRANT_PORT=6333
NEO4J_URI=bolt://localhost:7687
REDIS_HOST=localhost
REDIS_PORT=6379
```

### Plugin

Настройки доступны через: `File` → `Settings` → `Tools` → `AetherNexus`

---

## 🗺️ Roadmap

### Фаза 1: MVP ✅
- [x] Базовая структура плагина
- [x] Подключение к бэкенду
- [x] Простой текстовый поиск
- [x] Базовый UI (sidebar панель)

### Фаза 2: Основной функционал 🚧
- [ ] Семантический поиск
- [ ] "Explain this code"
- [ ] "Find related tickets"
- [ ] Контекстное меню

### Фаза 3: Расширенные возможности 📋
- [ ] Визуализация графа
- [ ] Генерация документации
- [ ] Оффлайн режим
- [ ] История и избранное

### Фаза 4: Полировка 📋
- [ ] Оптимизация производительности
- [ ] Улучшение UI/UX
- [ ] Расширенная документация
- [ ] Подготовка к публикации

---

## 🤝 Вклад в Проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Fork репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📝 Лицензия

Этот проект лицензирован под MIT License — см. файл [LICENSE](LICENSE) для деталей.

---

## 📧 Контакты

- **Email**: support@aethernexus.com
- **Website**: https://aethernexus.com
- **Issues**: https://github.com/aethernexus/aethernexus/issues

---

## 🙏 Благодарности

- IntelliJ Platform SDK
- FastAPI
- Qdrant
- Neo4j
- Всем контрибьюторам проекта

---

**Сделала с ❤️ для разработчиков Viktoriya Gonchar**
